import { redirect } from "next/navigation";

/** Deep AI analysis is deferred until the Codex research-job workflow exists. */
export default function AnalyzerPage() {
  redirect("/opportunity-lab");
}
